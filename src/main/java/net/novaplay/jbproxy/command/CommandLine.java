package net.novaplay.jbproxy.command;

import net.novaplay.api.command.CommandArgument;

import java.util.*;

public class CommandLine extends net.novaplay.api.command.CommandLine{
	
	public CommandLine() {this(0);}
	public CommandLine(int id) { super(id);}
	
}
